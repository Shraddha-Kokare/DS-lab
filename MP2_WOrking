section .data
str1 db "Enter string: "
str1len equ $-str1

str2 db "Your entered string: "
str2len equ $-str2


section .bss

res resb 16
reslen resb 2
length resb 2

global _start

section .text

_start:
	mov rax,1
	mov rdi,1
	mov rsi,str1
	mov rdx,str1len
	syscall
	
	mov rax,0
	mov rdi,0
	mov rsi,res
	mov rdx,15
	syscall
	
	;mov rax,1
	;mov rdi,1
	;mov rsi,str2
	;mov rdx,str2len
	;syscall
	
	;mov rax,1
	;mov rdi,1
	;mov rsi,res
	;mov rdx,reslen
	;syscall
	
	dec rax
	mov rbx,rax
	mov rcx,16H
	mov rdi,res
	
_loop:
	rol rbx,04H
	mov al,bl
	and al,0FH
	cmp al,09
	jg _b2
	add al,30H
	jmp _b3
	
_b2:
	add al,37H

_b3:	
	mov byte[rdi],al
	inc rdi
	dec rcx
	jnz _loop
	

	mov rax,1
	mov rdi,1
	mov rsi,res		
	mov rdx,16
	syscall

	
	
	mov rax,60
	mov rdi,00
	syscall
